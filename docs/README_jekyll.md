# Installer Jekyll

Installer Ruby :

```bash
sudo apt-get install ruby-full build-essential
```


Installer des gem uniquement pour l'utilisateur courant :

```bash
gem install --user-install bundler jekyll
```

TODO

# Installer les gem nécessaires

Le fichier `jekyll-theme-slate.gemspec` contient la liste des librairies nécessaires pour le thème.

```bash
gem install --user-install -g
```

